import './styles.sass'

const NotFound = () => {
    return (   
        <section className="not-found__container">
            <h1>No se ha encontrado el recurso</h1>
        </section>
    )
}

export default NotFound;